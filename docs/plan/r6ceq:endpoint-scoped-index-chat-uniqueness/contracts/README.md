# Contracts（#r6ceq）

本目录存放本计划涉及的接口契约文档，按 Kind 拆分：

- `config.md`：Settings 约束（chat_id 唯一）
- `file-formats.md`：本地索引路径与 remote index 的 endpoint-scoped 语义
- `cli.md`：受影响的 CLI 行为（如 backup run / index sync）

