# 接口契约（Contracts）

本目录存放本计划的接口契约。契约必须按 `Kind` 拆分文件，避免形状混杂导致后续计划无法增量演进。

编写约定：

- `../PLAN.md` 的 Inventory 表是唯一清单；所有接口必须在表内出现，并链接到本目录对应文件。
- 本计划只保留实际用到的契约文件；不涉及的类型不要创建。
- 修改既有接口时，契约必须写清楚：变化点（旧→新）、兼容性、迁移/rollout。

本计划契约文件：

- `cli.md`
- `events.md`
- `file-formats.md`
