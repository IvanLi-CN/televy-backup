# History

The settings window transport was consolidated on the daemon control socket after independent CLI
process termination could be displayed as `signal=9`. The contract intentionally keeps the
terminal CLI available while removing it from the app's daemon transport boundary.
