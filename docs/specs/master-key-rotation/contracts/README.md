# Contracts（#4fexy）

本目录存放本计划涉及的接口契约文档，按 Kind 拆分：

- `cli.md`：轮换命令与输入输出
- `config.md`：secrets store key 约定（active/pending/rotation_state）
- `events.md`：事件/进度 schema 增量
- `file-formats.md`：rotation state 与索引库双轨路径约定
