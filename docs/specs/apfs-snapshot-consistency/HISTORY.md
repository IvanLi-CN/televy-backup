# History

- Strict APFS snapshot consistency is introduced as a standalone topic.
- The validated permission baseline is FDA for both the final Snapshot Access
  app and the final root mount helper. This is an observed capability
  requirement, not an attempt to introspect TCC state.
